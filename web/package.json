{
  "name": "lerno-web-workspace",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter main dev",
    "build": "pnpm --filter main build",
    "start": "pnpm --filter main start"
  }
}
